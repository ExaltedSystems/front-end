import { BreakfastTypeFilterPipe } from './breakfast-type-filter.pipe';

describe('BreakfastTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BreakfastTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
