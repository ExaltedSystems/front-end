import { HotelFiltersPipe } from './hotel-filters.pipe';

describe('HotelFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new HotelFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
