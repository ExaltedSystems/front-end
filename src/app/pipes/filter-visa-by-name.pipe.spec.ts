import { FilterVisaByNamePipe } from './filter-visa-by-name.pipe';

describe('FilterVisaByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVisaByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
