import { SecondsPipePipe } from './seconds-pipe.pipe';

describe('SecondsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
